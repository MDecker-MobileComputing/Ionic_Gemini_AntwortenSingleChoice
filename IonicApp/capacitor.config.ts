import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.eldecker.ionic.antwortensinglechoice',
  appName: 'Gemini: SC-Antworten',
  webDir: 'www'
};

export default config;
